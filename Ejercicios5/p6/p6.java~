public class p6 {
    public static void main(String[] args) {
        for (int i = 0; i < 5; ++i) {}
        i = 1;
        /* The compiler can't find the symbol `x`.
         * There is no way to surpass this error. */
    }
}
